```
1. Arrow / Gecko Gameplay

Arrow.cs — [MOD]
- Added HoleWorldPosition property returning the off-grid exit-lane cell the arrow dives into (read by MapSpawner to place the pooled Hole)
- Exposed body-pattern instancing state and gecko-skin fields for the per-ColorId art path
- Added tutorial-lock support so inert arrows can suppress the blocked-tap feedback

Arrow.Func.cs — [MOD]
- Called ExtinguishTrailDots() on reversal so trail dots are killed immediately instead of finishing behind the retreating body
- Triggered head stun spin (PlayStunned) when the arrow hits a blocker and bounces home
- Spawned the per-arrow hole visual at level load

Arrow.Input.cs — [MOD]
- Gated blocked-tap feedback behind BlockedClickFeedbackSuppressed() so tutorial mis-taps stay silent
- Added re-tap-cancels-stun handling

Arrow.Visual.cs — [MOD]
- Added UpdateBodyPattern() computing per-motif transform matrices for GPU-instanced hoa_tiet body patterns (uniform scale, anchored to head, follows S-wave + hole-dive shrink)
- Added PatternVisible gate and EnsurePatternCapacity() growth for the matrix buffer, capped at 256 motifs
- Revealed hoa_tiet motifs progressively along the body during the intro fill
- Cleared _patternCount on spawn/return so a pooled arrow never draws stale motifs
- Wired per-ColorId head/body/pattern/limb art selection

ArrowHeadVisual.cs — [MOD]
- Added tongueRenderer child support: sorts one below the head, inherits head transform/pitch/scale, fixed red across every ColorId (not swapped by SetSprite)
- Added PlayStunned()/StopStunned() controlling the Stunned particle spin, auto-found in the head hierarchy when unwired
- Applied color/bounds via MaterialPropertyBlock to head and tongue together

ArrowPictureService.cs — [MOD]
- Added DrawBodyPatterns collecting per-arrow motif matrices and drawing them in one Graphics.DrawMeshInstanced batch from LateUpdate
- Added StopDotAt(cell) to snap a dot back to rest and drop it from the animating set on reversal
- Started shared crawl + hole-dive audio loops per arrow (owned handles stopped when the arrow stops)

HoleView.cs — [NEW]
- New IPoolable decorative hole placed at each arrow's exit-lane mouth, sorts under the gecko so the dive reads as going in
- Uses the prefab's default sprite material (not the bounds-clip mask) since the hole sits one cell off-grid

----------------------------------------------------------------------

2. Obstacle / Tutorial

IObstacleAbility.cs — [MOD]
- Declared SuppressBlockedClickFeedback() contract (default false)

ObstacleAbility.cs — [MOD]
- Added virtual SuppressBlockedClickFeedback() returning false so real obstacles still nudge

TutorialLockObstacleAbility.cs — [MOD]
- Overrode SuppressBlockedClickFeedback() to true so a tap on a not-yet-your-turn arrow stays silent (no flash, no "wrong" sound)

Tutorial First play Step 1.asset — [MOD]
- Set up the first tutorial step

----------------------------------------------------------------------

3. Board / Map

BoardGridOverlay.cs — [MOD]
- Drew the full grid lattice (every cell, occupied or empty) via a right/up-only pass covering each edge once, instead of only occupied cells

MapSpawner.cs — [MOD]
- Placed a pooled Hole visual per arrow at its HoleWorldPosition on level load and hid it on win
- Wired the per-ColorId arrow color path

----------------------------------------------------------------------

4. Audio

AudioPlayer.cs — [MOD]
- Added PlayLoop(clip) that loops until Stop() without auto-returning to the pool; caller owns the handle
- Added SetPaused(bool) to pause/resume a looping source without releasing it
- Added _loopClip release on Stop() and as a pool-return safety net (idempotent)

AudioService.cs — [MOD]
- Added PlayLoopSound(id) returning an owned AudioPlayer handle (null when sound is off or id missing)
- Guarded the missing/failed-clip case before PlayLoop to avoid a use-after-return on the pooled player

AudioKey.cs — [MOD]
- Added keys for the shared gecko crawl and hole-dive loop clips

audio.config.asset — [MOD]
- Registered GeckoWalk and Hole Enters clips

GeckoWalk.ogg — [NEW]
- Crawl loop clip

Hole Enters.ogg — [NEW]
- Hole-dive loop clip

relaxing-puzzle-music-cozy.ogg — [MOD]
- Updated background music track

----------------------------------------------------------------------

5. Shaders / Materials

BoundMaskTransparent.shader — [MOD]
- Added an alpha-rim flash path lighting only the sprite silhouette edge with _FlashColor, driven per-renderer via MPB (_FlashAmount 0→1→0); skipped entirely when _FlashAmount==0
- Added _RimWidth as a percent-of-sprite UV fraction (not texels) to fix an earlier texel-based rim collapsing to zero width
- Added _RimMode: 0 = alpha rim (sprites), 1 = UV-edge rim for the body LineRenderer tube
- Added #pragma multi_compile_instancing + UNITY_SETUP_INSTANCE_ID for the GPU-instanced body-pattern motifs
- Note: outline-only red rim-flash was tried, reverted (didn't render), then re-landed as a resolution-independent v2

GeckoSprite.mat / GeckoBody_LightBlue.mat — [MOD]
- Serialized the rim-flash shader properties on the gecko materials

GeckoBody_*.mat (16 colours) — [NEW/MOD]
- Added per-ColorId GeckoBody materials wiring all 16 gecko skin colours

AB_m.mat — [NEW]
- Material for the Stunned VFX

----------------------------------------------------------------------

6. Config / ScriptableObjects

ColorConfigSO.cs — [MOD]
- Added per-ColorId head sprite + body material fields for the arrow color path

ColorConfigSOEditor.cs — [MOD]
- Added inspector wiring for the new per-ColorId color fields

GeckoConfigSO.cs — [MOD]
- Added body-pattern (hoa_tiet) tuning fields: spacing, size, end inset, angle offset

GeckoConfigSO.asset — [MOD]
- Tuned gecko body/limb proportions and adjusted the gecko skin values

ColorConfigSo.asset — [MOD]
- Populated the 16 gecko colours and adjusted color values

PrefabsReferencesSO.cs / PrefabsReferencesSO.asset — [MOD]
- Registered the Hole prefab reference

----------------------------------------------------------------------

7. Prefab Updates

Hole.prefab — [NEW]
- Pooled hole visual for the gecko dive

Stunned.prefab — [NEW]
- Stun-spin particle effect parented under the head

Arrow.prefab / ArrowHead.prefab — [MOD]
- Added the GPU-instanced body-pattern setup, the tongue child renderer, and the stun effect reference

ClothElement.prefab — [MOD]
- Adjusted sorting order of the cloth element

LevelPausePopup.prefab / LevelCompletedPopup.prefab — [MOD]
- Fixed sibling/child order on the pause popup

Dot.prefab — [MOD]
- Adjusted color

----------------------------------------------------------------------

8. Art Assets

asset_gecko / asset_gecko-1 (16 colour skins) — [NEW]
- Added per-ColorId gecko art sets (head1/head2, body, hand, leg, tail, tongue, hoa_tiet, chong mat) for all 16 colours

hoa_tiet.png / body2.png — [NEW]
- Body-pattern motif and body textures

hole.png — [NEW]
- Hole sprite

BG_ingame.png — [MOD]
- Updated the in-game background art

----------------------------------------------------------------------

9. Project Settings / Environment

ProjectSettings.asset — [MOD]
- Bumped AndroidBundleVersionCode 8 -> 10
- Adjusted project settings for the color/config changes
```
