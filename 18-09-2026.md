# bird-story

```
1. Intro Flow / Scene Setup

SceneCatalog.cs — [NEW]
- Added ScriptableObject catalog resolving scenes by name (Bootstrap, later Intro + Gameplay)
- OnValidate syncs each SceneReference name from its SceneAsset in Editor

SceneReference.cs — [NEW]
- Added serializable scene pointer holding runtime m_SceneName plus an Editor-only SceneAsset
- SyncFromAsset() derives the scene name from the dragged SceneAsset

IntroSceneScope.cs — [NEW]
- Added Intro scene controller: scales m_Logo from m_StartScale to m_EndScale and fades alpha in over m_Duration
- Skips the first post-load frame so the large load-frame deltaTime does not fast-forward the tween
- Reworked handoff from SceneManager.LoadScene(Bootstrap) to fade the logo out then LoadGameplay() via SceneService
- Added m_FadeOutDuration and m_GameLoadDelay fields; replaced m_BootstrapDelay

GameContext.cs — [MOD]
- Changed Start() to load SceneCatalog.Intro via SceneManager instead of GameplaySceneName via SceneService
- Boots Bootstrap first so all services exist before the intro plays

GameContextSettings.cs — [MOD]
- Added SceneCatalog field to the settings asset

SceneCatalog.asset / GameContextSettings.asset — [MOD]
- Wired SceneCatalog with Intro and Gameplay references into GameContextSettings

Intro.unity — [NEW]
- Added Intro scene at build index 0

EditorBuildSettings.asset — [MOD]
- Registered Intro scene as the first build entry

----------------------------------------------------------------------

2. Tutorial

TutorialOverlayUI.cs — [MOD]
- Added FadeIn() and FadeOutAsync() to fade the whole overlay on lesson open and end
- Faded per-element (pointer, spotlight, description) instead of the root CanvasGroup, which brightens the PMA hand
- Added m_OverlayFadeDuration, SetOverlayAlpha() and KillOverlayFade(); overlay starts transparent until FadeIn

TutorialPointer.cs — [NEW method]
- Added SetFadeAlpha() applying an overall fade factor through the PMA-correct hand/trail color
- Split hand/trail alpha into logical value plus ApplyHandAlpha()/ApplyTrailAlpha() that multiply the fade factor

TutorialService.cs — [MOD]
- Awaits overlay.FadeOutAsync after MarkSeen, before hiding the overlay

GuidedMoveTutorial.cs — [MOD]
- Calls context.Overlay.FadeIn() when the lesson starts
- Stopped clearing hand/highlight at lesson end so the overlay fade owns disappearance

RotateTutorial.cs — [MOD]
- Calls context.Overlay.FadeIn() when the hand appears
- Added trailing period to the swipe description

SecondMoveTutorial.cs — [MOD]
- Reworded description to "Birds of different colors can't move to the same branch."

FirstMoveTutorial.cs — [MOD]
- Added trailing period to Description and DestinationDescription

TutorialOverlayUI.prefab — [MOD]
- Adjusted tutorial swipe wiring

----------------------------------------------------------------------

3. Rendering

RendererSpotlight.cs — [MOD]
- Added SetDimOpacity() with a _dimOpacity factor multiplied into the dim color alpha
- Added GetDimColor() so the dim material rebuilds its color from the current opacity

----------------------------------------------------------------------

4. UI Layer

GameScreenUI.cs — [NEW method]
- Added Space-key ToggleHud() that enables/disables m_Canvas for clean screenshots
- Guarded the toggle field, Update() and ToggleHud() with UNITY_EDITOR instead of a runtime Application.isEditor check
- Calls DebugRoot.Instance.SetBubbleVisible() so the debugger bubble hides with the HUD

----------------------------------------------------------------------

5. Debugger

DebugRoot.cs — [NEW method]
- Added SetBubbleVisible() toggling the floating bubble, respecting the ShowBubble setting

DebugSettings.asset — [MOD]
- Changed debug panel toggle key to C

----------------------------------------------------------------------

6. Gameplay

LevelRunner.cs — [MOD]
- Locked TreeRotateService and CameraPeekService in Init so drag deltas do not pile up during the opening intro
- Released both in CompleteIntro alongside the click service, fixing the tree snap and camera fling after intro

----------------------------------------------------------------------

7. Cheats

CheatService.cs — [NEW method]
- Added SetTimeScale()/ResetTimeScale() and TimeScale property backed by Time.timeScale, clamped 0..8

CheatPanelUI.cs — [NEW method]
- Added time-scale section: slider, label and reset button bound to CheatService
- SetupTimeScaleSection() syncs the slider without notify and RefreshTimeScaleLabel() shows the current factor

dev-tools.md — [MOD]
- Documented the time-scale cheat section

----------------------------------------------------------------------

8. Editor Tools

ScenePlayOverlay.cs — [MOD]
- Play now always opens build scene index 0
- Removed the start-scene dropdown, its EditorPrefs keys and the exit-play scene restore

BuildExecutor.cs — [MOD]
- Added Editor-embedded Android SDK as an adb lookup fallback (PlaybackEngines/AndroidPlayer/SDK) for Windows/Linux and macOS Hub layouts

----------------------------------------------------------------------

9. Config / Environment

manifest.json / packages-lock.json — [MOD]
- Added com.unity.ads.ios-support for the iOS ATT binding

ProjectSettings.asset — [MOD]
- Raised the Android target API
- Set the game icon (icon1024)

----------------------------------------------------------------------

10. Art / Assets

Logo (1)/ (Spine atlas, json, png, material, skeleton data) — [NEW]
- Added Logo Spine assets for the loading/intro logo

LoadingUI.prefab — [MOD]
- Fixed logo SkeletonGraphic glow when LoadingUI fades out

BirdVisualConfig.asset — [MOD]
- Adjusted bird animation config

Icon/icon1024.jpg — [NEW]
- Added 1024 game icon source

woodsign.png — [MOD]
- Updated woodsign obstacle texture
```
