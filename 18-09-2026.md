```
1. Intro Flow & Scenes

Intro.unity — [NEW]
- Added Intro scene that plays the logo animation before gameplay

SceneCatalog.cs — [NEW]
- New ScriptableObject holding Intro and Gameplay SceneReferences, resolved by name
- OnValidate syncs both references from their scene assets in the editor

SceneReference.cs — [NEW]
- New serializable type wrapping a scene asset plus its resolved scene name

SceneCatalog.asset — [NEW]
- Created the SceneCatalog asset and wired Intro and Gameplay scene entries

IntroSceneScope.cs — [NEW]
- Plays the logo (scale + fade) then loads gameplay via the persistent SceneService with useFade so LoadingUI fades in as the logo fades out
- Added HideLogoAsync that fades the logo, forces alpha 0, then waits one frame so the heavy scene-change frame leaves an empty background instead of a frozen logo
- Boostrap runs before Intro, so services already exist; drops the old direct SceneManager Bootstrap load

GameContext.cs — [MOD]
- Start now loads the Intro scene from SceneCatalog via SceneManager instead of loading gameplay from GameConfig.GameplaySceneName

GameContextSettings.cs — [MOD]
- Added a SceneCatalog field so GameContext can resolve Intro and gameplay scenes

GameContextSettings.asset — [MOD]
- Wired the SceneCatalog reference into the settings asset

EditorBuildSettings.asset — [MOD]
- Registered the Intro scene and reordered build scenes for a Boostrap-first boot

Intro art (Ellipse_5.png, loading.png, logo_cty.png) — [NEW]
- Added intro background and logo textures under Assets/Art/Intro

----------------------------------------------------------------------

2. Tutorial

TutorialOverlayUI.cs — [MOD]
- Added FadeIn and FadeOutAsync to fade the whole overlay (hand, spotlight, description) when a lesson opens and ends
- Fades per-element instead of via the root CanvasGroup because the PMA SkeletonGraphic hand would brighten additively under a CanvasGroup; drives hand/trail via the pointer, dim via SetDimOpacity, text via its own CanvasGroup
- Skipped the per-open description fade on first show so the overlay fade owns the appearance

TutorialPointer.cs — [MOD]
- Added SetFadeAlpha applying a PMA-correct alpha to the hand and trail

RendererSpotlight.cs — [MOD]
- Added SetDimOpacity so the spotlight dim can be faded with the overlay

TutorialService.cs — [MOD]
- Awaits FadeOutAsync after MarkSeen, before hiding the overlay

GuidedMoveTutorial.cs — [MOD]
- Calls Overlay.FadeIn on start and no longer stops pointing/clears highlight at the end, leaving cleanup to the overlay fade-out

RotateTutorial.cs — [MOD]
- Calls Overlay.FadeIn on start and leaves the hand up for the overlay fade-out

SecondMoveTutorial.cs — [MOD]
- Reworded the tutorial 2 description and added a trailing period

FirstMoveTutorial.cs — [MOD]
- Added trailing periods to the descriptions

TutorialOverlayUI.prefab — [MOD]
- Fixed the tutorial swipe hint wiring

----------------------------------------------------------------------

3. Dev Tools & Cheats

CheatService.cs — [MOD]
- Added SetTimeScale / ResetTimeScale / TimeScale, clamping Time.timeScale to 0..8

CheatPanelUI.cs — [MOD]
- Added a Time Scale section: slider, label, and reset button bound to CheatService, syncing slider and label to the running speed

dev-tools.md — [MOD]
- Documented the new time scale cheat section

ScenePlayOverlay.cs — [MOD]
- Play now always starts from build scene index 0 and exiting play no longer reopens a stored scene
- Removed the start-scene dropdown and its EditorPrefs keys (StartScenePrefKey, LastScenePathKey)

GameScreenUI.cs — [MOD]
- Added a Space-key toggle to hide/show the gameplay HUD by enabling/disabling its canvas, guarded with UNITY_EDITOR
- Hides the Debugger bubble via DebugRoot.SetBubbleVisible along with the HUD for clean screenshots

DebugRoot.cs — [MOD]
- Added SetBubbleVisible to hide or restore the floating debugger bubble, respecting the ShowBubble setting

DebugSettings.asset — [MOD]
- Changed the debug panel toggle key to C

----------------------------------------------------------------------

4. Gameplay

LevelRunner.cs — [MOD]
- Locks TreeRotateService and CameraPeekService in Init and releases them in CompleteIntro, so drag deltas no longer pile up during the intro and snap the tree / fling the camera in one frame

----------------------------------------------------------------------

5. UI Layer

LoadingUI.prefab — [MOD]
- Fixed the logo SkeletonGraphic glow when LoadingUI fades out

Logo (1) Spine assets (Logo.json, Logo.atlas.txt, Logo.png, materials, SkeletonData) — [NEW]
- Added the logo Spine skeleton, atlas, texture, and materials used by LoadingUI

----------------------------------------------------------------------

6. Build / Project Settings

BuildExecutor.cs — [MOD]
- Added the Editor-embedded Android SDK as an adb lookup fallback (PlaybackEngines/AndroidPlayer/SDK) for when Preferences are empty and the Editor is launched from the Dock without PATH

ProjectSettings.asset — [MOD]
- Updated the target API level and the game icon settings

icon1024.jpg — [NEW]
- Added the 1024 game icon under Assets/Art/UI/Icon

manifest.json — [MOD]
- Added com.unity.ads.ios-support for the iOS ATT binding

packages-lock.json — [MOD]
- Locked com.unity.ads.ios-support

----------------------------------------------------------------------

7. Config / ScriptableObjects

BirdVisualConfig.asset — [MOD]
- Updated bird animation settings

----------------------------------------------------------------------

8. Art

woodsign.png — [MOD]
- Updated the woodsign obstacle texture
```
