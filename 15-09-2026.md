# bird-story

```
1. Audio System

AudioKey.cs — [MOD]
- Added IceCrack (12) for a single ice decrement, separate from IceBreak (full shatter at zero)

AudioService.cs — [MOD]
- Added _flightCount to ref-count overlapping bird flights (branch move, intro fly-in, escape) so one flight ending does not cut another flight's BirdFlyLoop
- Subscribed OnLevelStart; reset _flightCount and stop the fly loop at level start
- Started BirdFlyLoop only on the first active flight, stopped it on the last
- Removed OnLevelComplete/OnLevelFail win/lose SFX handlers (moved to the result popups)
- Added a dedicated looping music AudioSource plus _musicClip/_musicVolume/_musicFadeDuration from config
- Started music once in the constructor so one BGM track loops uninterrupted across scene loads
- Fade music in from 0 on StartMusic; fade out via DOFade when Music is toggled off in Settings
- Subscribed OnMusicEnabledChanged and OnLevelStart; dropped the earlier LevelIntroStartedSignal / OnLevelResult music triggers

AudioConfigSO.cs — [MOD]
- Added MusicClip, MusicVolume (Range 0-1), MusicFadeDuration (Min 0) fields

AudioConfig.asset — [MOD]
- Wired ice.ogg to IceCrack and added the background-music clip entry

ice.ogg — [NEW]
- Ice-decrease crack SFX

148130378-playful-relaxed-holiday-theme-.ogg — [NEW]
- Looping background music track

----------------------------------------------------------------------

2. Analytics

AnalyticsService.cs — [NEW]
- Single game-side point touching Percas SDK; maps GameEventService ready/complete/fail to PercasSDK TrackGameStarted / TrackGameFinished
- Snapshot bird count at OnLevelReady; on loss report objectivesLeftPercent = birds remaining / birds at start
- TrackLevelGaveUp counts a mid-level replay as a loss so the APS funnel keeps a finish
- Every call is a no-op while PercasSDK is not initialized
- Added AnalyticsServiceLocatorExtensions.GetAnalyticsService

GameContext.cs — [MOD]
- Constructed and Subscribed AnalyticsService right after GameEventService
- Called TrackApplicationLaunch in Awake
- Added OnApplicationPause to send OnApplicationResume only after a real background pause (guarded by _wasPaused)

GameScreenUI.cs — [MOD]
- Called TrackLevelGaveUp on the replay button before reloading the scene

----------------------------------------------------------------------

3. Percas SDK (vendored import)

Percas SDK 0.0.6 — [NEW]
- Imported the Percas SDK package (Runtime + Editor asmdefs, PercasSDK entry point, installer prefab, config SOs)
- Bundled provider vendors: Adjust, Facebook, GameAnalytics, Firebase (analytics/config/crashlytics/messaging)
- Added PercasSDKConfig assets (Adjust/Facebook/Firebase) and GoogleService-Info.plist

Percas.SDK.Editor.asmdef — [MOD]
- Fixed the editor assembly definition and added the generated Firebase build files

ProjectSettings.asset — [MOD]
- Applied SDK-required project settings

----------------------------------------------------------------------

4. Notifications

Assets/Notifications/*.cs — [NEW]
- Ported the local notification module from tile-city, remapped Percas.Core.* to Percas.BirdStory.*
- INotificationScheduler hides all native API; Android/iOS/Null implementations keep every #if inside their own file
- NotificationService reschedules 14 discrete daily 20:30 reminders on background, starting tomorrow; feature-teaser track A falls back to comeback track B past level 31
- NotificationData persists MaxLevelCleared / PermissionGranted / PermissionAsked via PlayerPrefs (key data_notification)
- NotificationBootstrap auto-spawns a persistent GameObject and hooks OnApplicationPause for reschedule
- NotificationDebugTools auto-spawns an OnGUI panel in Editor / Development Build
- Permission requested once at the Win screen after clearing level 3, gated on the Next button

notification-service-phase1-v2.md — [NEW]
- Phase 1 spec doc for the ported module

CheatService.cs — [MOD]
- Synced Debug_SetMaxLevelCleared to CurrentLevel-1 on GoToLevel so notification content tracks cheat jumps

LevelCompletePopup.cs — [MOD]
- Added the NOTIFICATION define guard and Percas.Notifications using

manifest.json / packages-lock.json — [MOD]
- Added com.unity.mobile.notifications 2.3.2

NotificationsSettings.asset — [NEW]
- Enabled reschedule on device restart

ProjectSettings.asset — [MOD]
- Enabled the NOTIFICATION define for Android/iPhone

----------------------------------------------------------------------

5. Tutorial

TutorialPointer.cs — [MOD]
- Replaced m_SwipeBottomMargin with a 9-point TextAnchor m_SwipeAnchor plus a Vector2 m_SwipeMargin, computing the swipe center from the anchored edge
- Removed SwipeBaseY; added SwipeAnchorPoint

TutorialOverlayUI.cs — [MOD]
- Added SetDescriptionFade and a CanvasGroup crossfade: fade the old description out, swap text mid-fade, fade the new one in
- Added m_DescriptionGroup, m_DescriptionFadeDuration; kill the running tween on OnDestroy and BeforeHide

GuidedMoveTutorial.cs — [MOD]
- Added virtual FadeDescription (default off); pass it to Overlay.SetDescriptionFade before showing the description

FirstMoveTutorial.cs — [MOD]
- Enabled FadeDescription for the first move; edited the destination hint text ("bird" to "birds")

TutorialOverlayUI.prefab — [MOD]
- Tutorial layout adjustments and description CanvasGroup wiring

Tutorial.mat — [NEW]
- Tutorial text material

----------------------------------------------------------------------

6. Gameplay Features

IceFeature.cs — [MOD]
- Played IceCrack SFX on each ice decrement before it shatters
- Gated HideModel and the shatter FX on m_BirdRevealLead instead of m_BirdRevealLead + m_BirdPopDuration, removing RevealAnimationDuration, so the ice shatters exactly as the bird emerges

----------------------------------------------------------------------

7. Level Intro & Camera

LevelIntroService.cs — [MOD]
- Published BirdsFlightStartedSignal / BirdsFlightEndedSignal around the intro fly-in (and closed the signal on Clear) so the fly loop covers the intro flight
- Added IntroPitchDelay: hold the build angle for _pitchDelay before easing to rest pitch, offsetting TickPitch progress
- In Prepare, for IntroSpinImmediate rotate the tree straight to the target while loading still covers the screen, removing a one-frame snap on the first Tick

RotateConfigSO.cs — [MOD]
- Added IntroPitchDelay field (ignored when IntroPitchImmediate)

LevelRunner.cs — [MOD]
- Added LastFixedCameraLevel (2); hard-lock tree rotation and camera peek for levels 1-2 at level start, independent of tutorial state

CameraPeekService.cs — [MOD]
- Added a per-level _hardLocked separate from the transient _locked; IsLocked ORs both; SetHardLocked; extracted StopDrag

TreeRotateService.cs — [MOD]
- Added _hardLocked / SetHardLocked mirroring CameraPeekService; extracted StopMotion

BirdEscapeService.cs — [MOD]
- Published BirdsFlightStartedSignal when the escape flight opens and BirdsFlightEndedSignal when the last bird leaves or on Clear

----------------------------------------------------------------------

8. Bird Selection / Motion

BirdAnimation.cs — [MOD]
- Replaced PlayBounce with PlayLift + PlayHover (rise then looping yoyo bob) and PlayDrop (fall back to slot)
- Guarded OnKill so handing the rise tween to the hover loop does not null the wrong _tween

BirdElement.cs — [MOD]
- Replaced BeginSelectBounce with BeginSelectLift (rise + hover, fly clip kept playing) and EndSelectLift (drop, return to idle)

SelectionService.cs — [MOD]
- Called BeginSelectLift on select; on deselect call EndSelectLift and track birds until they land

BirdMotionConfigSO.cs — [MOD]
- Added SelectHoverHeight, SelectHoverDuration, SelectHoverEase; reworded SelectEase doc

BirdMotionConfig.asset — [MOD]
- Populated the new hover fields

----------------------------------------------------------------------

9. Shaders & Outline

UnlitTextureOutline.shader — [NEW]
- Stencil outline in one material: MASK marks the silhouette (ZTest Always), BASE draws the texture, OUTLINE extrudes along normals and draws where the stencil differs, ZWrite On so it blocks the Screen Space - Camera background
- Added the _SMOOTH_NORMALS keyword: OUTLINE reads baked smooth normals from UV3, falling back to the vertex normal when unbaked

SmoothNormalBaker.cs — [NEW]
- AssetPostprocessor that, for models tagged with the SmoothOutline label, averages normals per quantized vertex position and writes them to UV3

SmoothNormalBakerWindow.cs — [NEW]
- Editor window (Tools/BirdStory/Smooth Normal Baker) to enable/disable and re-bake per model via the label

ObstacleOutline.mat / ObstacleKeyOutline.mat — [NEW]
- Obstacle outline materials on the new shader

BirdOutline.mat / IceObstacle.mat — [MOD]
- Wired to the outline shader; IceObstacle enables the smooth-normal keyword

HiddenFeature.prefab / KeyFeature.prefab — [MOD]
- Wired obstacle outlines; updated the KeyFeature effect

----------------------------------------------------------------------

10. UI

GameScreenUI.cs — [MOD]
- Wrapped the level label in m_LevelSlide; slide it in on level start and hide it with the setting/replay buttons on result

UIService.cs — [MOD]
- Raised PopupBaseSortingOrder from 100 to 1000 so popups render above overlays such as the tutorial (300)

GameScreenUI.prefab — [MOD]
- Added the level-label SlideUI and refreshed frame/button art

Art (frame_button_blue.png, frame_popup.png, frame_title.png, icon_music.png, LilitaOne outline mat) — [MOD/NEW]
- UI frame art fixes and the Settings music icon

SettingPopup.prefab / SettingGroup.cs — [MOD]
- Added an optional Music toggle backed by IsMusicEnabled, guarded until wired into the prefab

Game.unity — [MOD]
- Scene wiring for the background music setup
```
