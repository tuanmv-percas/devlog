```
1. Gameplay - Win Sequence

ArrowPictureService.cs - [MOD]
- Added a winPanelDelay beat between the win anim (camera reset + dot vanish) finishing and the win panel pushing up, so the cleared board reads for a moment before the popup lands
- Guarded the new delay with DelayOrCancel and a null-check fallback (0.35f) before invoking EventService.OnLevelWin

GameplayConfigSO.cs - [MOD]
- Added winPanelDelay field (default 0.35f) with tooltip for the post-win-anim, pre-panel beat
- Retuned win dot-vanish defaults: winDotVanishHold 0.35 -> 0.5, winDotVanishDuration 0.25 -> 0.4, winDotVanishWaveDelayPerUnit 0.03 -> 0.05, winCameraResetDuration 0.35 -> 0.6

----------------------------------------------------------------------

2. In-Game HUD

InGameScreen.cs - [MOD]
- Added ReplayHiddenLevelCount (=4) and ShouldShowReplay(): Restart now hidden on displayed Level 1-4 in addition to the FirstPlay tutorial, since early players mis-tap it before learning the board
- Replaced the OnLoadLevel lambda with HandleLoadLevel(), which refreshes the level label and re-evaluates Restart visibility per level
- Changed win-sequence HUD teardown from Hide() (fade) to HideImmediate() in OnAllArrowsClearedHud so the HUD disappears the instant the timer freezes instead of lingering a beat
- OnTutorialEnded now calls SetReplayVisible(ShouldShowReplay()) instead of forcing Restart on, so the first-few-levels gate still applies after the tutorial

----------------------------------------------------------------------

3. Core UI

Presentation.cs - [MOD]
- Added HideImmediate(): hides a Presentation instantly, skipping every ITransition (cancels show/hide CTS, zeroes CanvasGroup alpha/interactable/blocksRaycasts, deactivates the GameObject, fires and clears the hide callback)

----------------------------------------------------------------------

4. Level Complete

LevelCompletedPopup.cs - [MOD]
- Added a ParticleImage confetti [SerializeField] and fired it when the Spine 'appear' animation completes (OnAppearComplete), so the burst syncs with the popup entrance instead of firing at alpha 0
- Added ResetConfetti() at StartShow to stop emission and clear leftover particles, so a pooled reuse no longer shows the previous run's confetti
- Added PlayConfetti(), also fired directly when winAnimation is null so the celebration still plays

LevelCompletedPopup.prefab - [MOD]
- Wired the ParticleImage confetti reference into the popup prefab

----------------------------------------------------------------------

5. Map Spawner

MapSpawner.cs - [MOD]
- Added ValidateSpawnDependencies() guard run at the top of SpawnMap: checks arrowPictureService, LevelData, GameplayConfigSo, PoolService, PrefabsReferences and PrefabsReferences.ArrowPrefab, naming every missing dependency in one Debug.LogError line instead of throwing a bare NRE that hangs the Splash preloader
- Recorded the failure as a Crashlytics non-fatal via PercasSDK.Crashlytics.RecordException (guarded by PercasSDK.IsInitialized) so the choke point stays visible on players' devices
- Added _dependencyErrorReported flag so repeated SpawnMap calls on the same broken state don't spam the log / inflate the non-fatal count; cleared on the next successful validation
- Added Percas.SDK using directive

----------------------------------------------------------------------

6. Config / ScriptableObjects

GameplayConfigSO.asset - [MOD]
- Added arrowIntroRevealSoftness (0.03) and the win dot-vanish block (winDotVanishHold, winDotVanishDuration, winDotVanishWaveDelayPerUnit, winDotVanishEase), winCameraResetDuration and winPanelDelay
- Retuned live win values: winDotVanishDuration 0.4 -> 0.2 and winDotVanishWaveDelayPerUnit 0.1 -> 0.05 for a 2x faster board clear on win; winTriggerDelay 0.35 -> 0.25

----------------------------------------------------------------------

7. Build / Editor Tooling

BuildExecutor.cs - [MOD]
- Rewrote FindAdb() to resolve adb to an absolute path (Unity SDK -> env -> default per-OS SDK dirs -> manual PATH scan), returning null when not found, since Process.Start with UseShellExecute=false does not search PATH and Unity launched from Finder/Dock does not inherit the shell PATH
- Wrapped the adb install Process.Start in try/catch, logging the failing adb path instead of throwing Win32Exception

----------------------------------------------------------------------

8. Percas SDK

AdbEventMonitorWindow.cs - [NEW]
- New Editor window (Percas > ADB Event Monitor), portable across games: runs adb by bundle (install / DebugView toggle / cold-start), streams analytics-tag logcat into the Editor with filter, only-events and auto-scroll
- Added device selector (-s serial) and clear-on-start; Editor-only, does not touch runtime

ParticleImage.Editor.asmdef - [NEW]
- Added assembly definition for ParticleImage.Editor (imported with the ParticleImage package)

----------------------------------------------------------------------

9. Third-party Assets

ParticleImage 2/ - [NEW]
- Imported the AssetKits ParticleImage package (Runtime, Editor, Jobs, demo scenes/prefabs/sprites) referenced by the LevelCompletedPopup confetti

----------------------------------------------------------------------

10. Docs

docs/Gameplay.md - [MOD]
- Updated win-sequence timing docs to match the faster dot-clear and instant HUD-hide changes

----------------------------------------------------------------------

11. Cheat / Creative Capture Tools (branch creative/ct-1)

CreativeController.cs - [NEW]
- New editor-only (#if UNITY_EDITOR) self-spawning singleton (DontDestroyOnLoad, not placed in any scene) that drives ad/creative capture; never compiled into device or release builds
- Toggle/enable the HandCursor, hide the OS cursor (Cursor.visible) while active, and load the HandCursor prefab via AssetDatabase so the asset does not ship
- Set/step hand-cursor scale clamped to MinScale 0.3 - MaxScale 3 in ScaleStep 0.1 increments
- Hide the InGameScreen HUD by driving its CanvasGroup alpha via a runtime reference, without touching any gameplay file

CheatPanel.cs - [MOD]
- Added an editor-only CREATIVE toolbar built at runtime by cloning jumpButton (no prefab edits), pinned as a top-center horizontal strip so it never overlaps the existing cheat buttons
- Laid out buttons left-to-right: Cursor toggle, HUD toggle, size [-], size display, size [+]; the size display cell has raycastTarget off (display-only)
- Added RefreshCreativeLabels() with ASCII labels (game TMP font lacks some Vietnamese glyphs); BuildCreativeControls on init, RefreshCreativeLabels on StartShow

HandCursor.cs - [MOD]
- Ported from tile-city-2: kept field names so existing prefab wiring stays valid
- Added canvas-rect fallback (GetComponentInParent<Canvas>) when m_CanvasRect is unassigned
- Reordered Update to follow mouse/touch first so a broken anim clip no longer blocks cursor movement; added null-guard on m_HandAnimation
- Added SetScale(float) that scales the whole root so the tip keeps pointing correctly
- Consolidated build guards: only Awake -> SetActive(false) survives outside UNITY_EDITOR

InGameScreen.cs - [MOD]
- Changed the editor-only cheat-panel dev shortcut from KeyCode.P to KeyCode.X
```
