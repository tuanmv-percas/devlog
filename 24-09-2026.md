```
1. Services

BlockDispatchService.cs — [MOD]
- Added HandleStickyClickAsync so a sticky pair walks one leader path and lands in two slots
- Added BuildPartnerPath that appends the leader cell and drops the leader's last cell
- Added TryTakeAdjacentSlotsAsync that compacts the slot row when two adjacent free slots are missing
- Copied FindExitPath output into _leaderPath and _partnerPath before the buffer is reused
- Added _flyingCount and ResetForLevel() to count blocks in the air and zero it per level
- Changed Detach to return void and removed the per-partner FindExitPath call
- Changed FlyToSlotAsync to drop the CellView and slot arguments and wrap the fly in _flyingCount

SlotsService.cs — [MOD]
- Added TryGetAdjacentFreeSlots returning two neighbouring empty slots
- Removed TryGetFreeSlots(count) in favour of the adjacent lookup
- Added CompactSlots that slides blocks to the head of the row over CompactDuration
- Added MoveBlock that reparents a block to a new slot and fires OnBlockMoved
- Added FindSlot(block) to locate the slot holding a block
- Added OnBlockMoved event
- Changed FreeSlotAsync to detach the glue at clear start and re-resolve the owning slot after the anim

----------------------------------------------------------------------

2. Piece Movement

PieceMovementService.cs — [MOD]
- Added HandleBlockMoved with RebindPending, RebindWaves and RebindAgents to move in-flight pieces to the new slot
- Added TryFreeSlot and StickyPartnerSlot so a sticky pair frees both slots in one frame
- Replaced CaptureNearest with CollectCapturable that gathers every in-range piece in one flock scan
- Added InsertCapturable keeping the capture list sorted by remaining distance
- Changed the drain loop to iterate the collected list instead of rescanning the flock after each capture
- Changed the radius test to !(remaining <= radius) so a NaN distance stays out of range
- Subscribed to SlotsService.OnBlockMoved in the constructor

PieceMoveAgent.cs — [MOD]
- Added enterTween field holding the capture tween so its target can change when a block moves slot
- Stopped enterTween in ResetForReuse

PieceMovementConfig.cs — [MOD]
- Documented that lowering neighborBucketSize changes the neighbour set and the flock's path

----------------------------------------------------------------------

3. Elements

BlockElement.cs — [MOD]
- Added SettleIntoSlot that slides the block to its slot origin over a duration
- Added _settleTween field
- Changed MoveToSlotAsync to fly to LocalPosition zero and dropped the SlotView argument
- Changed the fly arc to lift m_ModelRoot along negative Z instead of local Y

----------------------------------------------------------------------

4. Elements / Features

BlockSticky.cs — [MOD]
- Added RefreshLink that aims and stretches the glue every frame from the two block models
- Added LinkPoint and ModelRootOf helpers
- Renamed AimLink to SetupLink and moved m_LinkRoot under the block's model root at init
- Added _baseDistance and _appliedLength to measure glue length in the parent's local space

----------------------------------------------------------------------

5. Controllers

GameController.cs — [MOD]
- Added HandleBlockMovedSlot that carries the arrived flag from the old slot to the new one
- Subscribed and unsubscribed SlotsService.OnBlockMoved
- Added _blockDispatchService.ResetForLevel() on level start
- Changed AnySlotBlockPending so a box waiting on a sticky partner defers to the partner's row

----------------------------------------------------------------------

6. Config / ScriptableObjects

SlotsConfig.cs — [MOD]
- Added CompactDuration field for the slot-row compaction slide

BlockAnimConfig.cs — [MOD]
- Documented FlyJumpPower as a height along the Z axis

BlockAnimConfig.asset — [MOD]
- Changed FlyJumpPower from 1.5 to 5.7 for the Z-axis lift
- Added SuperPulseScale 1.15 and SuperPulseDuration 0.35
```
