```
1. Arrow / Gecko Gameplay

Arrow.cs — [MOD]
- Added HoleExitOffset accessor reading GeckoConfigSO.holeExitOffset (falls back to 0)
- Added _holeView field and AttachHole(HoleView) so FinishArrow can fire that hole's dive-impact VFX
- Cleared _holeView in ResetRuntimeState so a pooled arrow never points at a returned hole
- Added AreHeadIdleGesturesSuppressed() returning true while the arrow is ability-blocked (Ice, Lock, TutorialLock) or covered by ClothService
- Cached ClothService through ServiceLocator.TryGet on first use
- Exposed HandSprite/LegSprite, the limb matrix buffers and LimbsVisible for the instanced limb draw

Arrow.Func.cs — [MOD]
- Applied HoleExitOffset to _holeDistance in ComputeHole, in both the walked exit-lane branch and the no-path fallback, so the dive mouth and the hole sprite shift together
- Called _holeView.PlayDiveImpact() in FinishArrow when HoleDiveEnabled, before notifying the service, then set _holeView to null
- Replaced the per-arrow limb SpriteRenderer updates with UpdateLimbs/ComputeLimbPair matrix computation

Arrow.Visual.cs — [MOD]
- Replaced the per-ColorId head sprite (ColorConfig.headSprite) with ArrowHeadVisual.ApplySkin(_colorId), selecting that colour's Spine skin
- Passed the gesture-suppression predicate to the head via SetGestureSuppression during head setup
- Rewrote UpdateBodyPattern to anchor motifs at both ends (first at the head inset, last at the tail inset) with count = round(span/spacing)+1 and step = span/(count-1), so the tail no longer drops a motif
- Added a single centred motif when the usable span is shorter than one spacing, and an early return with _patternCount = 0 when the span is <= 0
- Removed the 4 limb SpriteRenderers and their per-renderer MaterialPropertyBlocks in favour of computed limb matrices

ArrowHeadVisual.cs — [MOD]
- Replaced the static head SpriteRenderer with a Spine SkeletonAnimation looping 1_idle_tinh
- Played one idle gesture every spineTongueIntervalMin..Max seconds, random 50/50 between 3_the_luoi and 2_chop_mat
- Added ApplySkin(ColorId) with the baked SkinByColorId permutation (skins "1".."16" are not in ColorId order), overridable through GeckoConfigSO.spineSkinOverrides
- Played 4_choang at spineStunTimeScale for stun, replacing the old ParticleSystem stun spin
- Made SetFlash and SetBoundsMask no-ops — the Spine material carries no bounds-clip or rim-flash
- Froze the skeleton on TimeService pause
- Added SetGestureSuppression(Func<bool>) plus _wasSuppressed: holds the base idle and plays no gestures while suppressed, then resumes with a fresh interval on the frame it clears
- Cleared _gesturesSuppressed and _wasSuppressed in OnReturnToPool

ArrowPictureService.cs — [MOD]
- Moved the OnAllArrowsCleared invoke out of OnArrowCleared into CelebrateThenWinAsync, fired after a GameplayConfigSO.winTriggerDelay wait that honours the win CancellationToken, so HideHoles no longer pulls the holes on the same frame the last tail is swallowed
- Added DrawLimbs() to LateUpdate, bucketing every arrow's limb matrices by hand/leg sprite into one Graphics.DrawMeshInstanced per distinct sprite
- Added _limbResBySprite and EnsureLimbRes with LimbRenderQueue 2999 so instanced limbs draw under the body tube
- Set the picture-bounds min/max on the shared limb material once per frame instead of a per-limb MaterialPropertyBlock
- Destroyed the limb materials and quads alongside the pattern resources on teardown

CameraScaleHandler.cs — [MOD]
- Widened the per-side hole border by holeExitOffset converted to cells (Mathf.Max(0, offset) / cellSize) so a pushed-out hole stays framed; a negative offset clamps to the existing 1-cell border

----------------------------------------------------------------------

2. Hole / Dive Impact VFX

HoleView.cs — [MOD]
- Added PlayDiveImpact(): squash-pop on the sprite transform plus a ParticleSystem puff, gated by impactEnabled
- Added serialized impactEnabled, squashPunch (1.18), squashDuration (0.28), impactPuff and tintPuffWithArrowColor fields
- Added SetDustColor(Color) so the puff's startColor is tinted to the diving gecko's colour
- Built the squash-pop as a PrimeTween Sequence (OutQuad up over 35% of the duration, OutBack back over 65%)
- Stored the Sequence in _squashSeq and stopped the parent Sequence instead of calling Tween.StopAll on the target, which errored on PrimeTween nested tweens when HideHoles returned holes to the pool on win
- Cached _visual and _visualBaseScale in Awake and restored the resting scale in OnGetFromPool and OnReturnToPool
- Stopped the puff with StopEmittingAndClear and dropped _arrowColor on pool return

MapSpawner.cs — [MOD]
- Captured the arrow's ColorConfig colour into arrowColor and passed it to hole.SetDustColor so the dust matches the gecko
- Called arrowGo.AttachHole(hole) so FinishArrow can fire that hole's dive-impact VFX

Hole.prefab — [MOD]
- Carries the ImpactPuff ParticleSystem child built by HolePuffSetup, wired into HoleView.impactPuff

HoleDust.mat — [NEW]
- Sprites/Default material for the puff, mainTexture pointed at the hole sprite so the dust reads as hole debris

----------------------------------------------------------------------

3. Obstacle / Audio

IceObstacleAbility.cs — [MOD]
- Played AudioKey.IceBreak once per ice-line segment in RemoveIceLineTailPoint, so the shatter sounds shard-by-shard
- Cached AudioService through ServiceLocator on first use

AudioKey.cs — [MOD]
- Added the IceBreak = "ice_break" constant

audio.config.asset — [MOD]
- Mapped ice_break to ice_break2.ogg at volume 0.5 with random pitch 0.9–1.1 so the rapid repeats do not read as one burst

ice_break2.ogg — [NEW]
- Ice shatter SFX clip

----------------------------------------------------------------------

4. Config / ScriptableObjects

GeckoConfigSO.cs — [MOD]
- Added holeExitOffset (world units along the exit lane, default 0) under a new Hole header
- Added the Head Spine block: spineIdleAnim, spineTongueAnim, spineBlinkAnim, spineStunAnim, spineStunTimeScale (0.5), spineTongueIntervalMin (4) / Max (8) and spineSkinOverrides[]

GameplayConfigSO.cs — [MOD]
- Added winTriggerDelay (0.35s) under a new Win Timing header

ColorConfigSO.cs — [MOD]
- Marked headSprite as no longer used — the head look is now a Spine skin — and kept the field to avoid data churn

GeckoConfigSO.asset — [MOD]
- Set holeExitOffset to 0.2
- Filled the Spine head values: 1_idle_tinh / 3_the_luoi / 2_chop_mat / 4_choang, stun time scale 0.5, gesture interval 2–4s, no skin overrides

----------------------------------------------------------------------

5. UI Layer

InGameScreen.cs — [MOD]
- Added an editor-only P key branch in Update that pushes CheatPanel, marks the cheat unlocked, fires OnCheatPanelOpened and resets the corner-tap counter, bypassing the tap sequence

InGameScreen.prefab — [MOD]
- Adjusted layout for the new level frame and HUD icons

PreloaderScreen.prefab — [MOD]
- Dropped the layered loading-background objects in favour of the single shader-driven background

----------------------------------------------------------------------

6. Editor Tools

HolePuffSetup.cs — [NEW]
- Idempotent Tools/Gecko/Setup Hole Puff VFX menu item that builds or reconfigures the ImpactPuff ParticleSystem child on Hole.prefab and assigns it to HoleView.impactPuff through SerializedObject
- Configured as a flat 2D fan: Circle shape at arc 180 and radius 0.05, burst of 6, lifetime 0.4, startSize 0.22, speed 1.0–1.8, no gravity, size and alpha easing to 0
- Creates or reuses HoleDust.mat (Sprites/Default) and points its mainTexture at the hole sprite; renderer set to Billboard, Default sorting layer, order 10

ArrowHeadSpineSetup.cs — [NEW]
- Idempotent Tools/Gecko/Setup Arrow Head Spine menu item wiring the Gecko SkeletonAnimation into ArrowHead.prefab

----------------------------------------------------------------------

7. Notifications

AndroidNotificationScheduler.cs — [MOD]
- Guarded GetAndroidSdkInt with #if !UNITY_EDITOR and returned 0 in the Editor, where the JNI lookup threw "Field SDK_INT not found"; Editor now skips the runtime permission path

----------------------------------------------------------------------

8. Preloader Background

PreloaderBackground.shader — [NEW]
- New GeckoJam/PreloaderBackground UI shader compositing the bg_loading / bg_loading1 gradients with a tiled diamond lattice from dot1/dot2
- Derives tiling from the screen aspect, with _AspectOverride to pin it, so the diamonds stay square
- Exposes _BandCount, _DotDensity, _DotScale, _DotOffset, _DotOpacity and _DotAltMode (0 = per-column alternation, 1 = checker, 2 = off)
- Keeps the full uGUI stencil/ColorMask property block so it still works under a Mask

PreloaderBackground.mat — [NEW]
- Material wiring the 4 sprites into the shader

bg_loading1.png / dot1.png / dot2.png — [NEW]
- Second gradient and the two diamond lattice tiles

bg_loading.png / loading_bar.png / loading_bar2.png / logo 1.png — [MOD]
- Updated preloader art

----------------------------------------------------------------------

9. Art & Assets

Spine Anim/Gecko (1)/ (Gecko.json, Gecko.atlas.txt, Gecko.png, Gecko_Atlas, Gecko_Material, Gecko_SkeletonData) — [NEW]
- Gecko head Spine skeleton with 16 colour skins and the 1_idle_tinh / 2_chop_mat / 3_the_luoi / 4_choang animations

ArrowHead.prefab — [MOD]
- Swapped the head SpriteRenderer for the Gecko SkeletonAnimation

frame_level_ingame.png / icon_replay.png / icon_setting.png — [MOD]
- Updated the in-game level frame and HUD icons

icon1024.png — [MOD]
- Updated the app icon

----------------------------------------------------------------------

10. Docs

docs/Gameplay.md — [MOD]
- Documented holeExitOffset and its camera framing, the hole dive-impact VFX, winTriggerDelay, the ice_break SFX, the Spine head (skins, idle gestures, stun), the both-ends-anchored body pattern and the GPU-instanced limbs

----------------------------------------------------------------------

11. Project Settings

DynamicsManager.asset — [MOD]
- Changed m_Gravity from (0, -9.81, 0) to (0, 0, 9.81) so the hole puff particles fan out flat on the board's XY plane instead of falling
```
