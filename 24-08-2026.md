# gecko-jam

```
1. Gameplay — Arrow & Hole

HoleView.cs — [MOD]
- Renamed PlayDiveImpact() to Dismiss(bool playImpact): plays the dive VFX then returns the hole to the pool, so each hole retires the moment its own arrow dives in instead of lingering until win
- Replaced the squash-pop with a pop-out (PopOut): scale-up to popOutScale (~1.35, Ease.OutBack) grouped with an alpha fade to 0, then disables the SpriteRenderer
- Renamed serialized fields squashPunch/squashDuration to popOutScale/popOutDuration via FormerlySerializedAs; added popOutFade and dismissLinger
- Added PuffLifetime() and a _dismissTween self-return delay = max(popOutDuration, puffLifetime) + dismissLinger, so the pooled object outlives impactPuff before Return()
- Stopped _dismissTween in OnReturnToPool so a pending self-return can't fire on a re-fetched hole
- Added BeginDive() looping scale-pulse (diveScale ~1.06, diveScalePeriod) during the dive plus EndDiveJuice() to stop it; added Dive-In Juice header fields (diveJuiceEnabled/diveScale/diveScalePeriod)
- Added SetSpriteVisible/SetSpriteAlpha and _baseColor capture; OnGetFromPool/OnReturnToPool re-enable the renderer and restore alpha for reuse

Arrow.Func.cs — [MOD]
- Changed FinishArrow to call _holeView.Dismiss(HoleDiveEnabled) instead of PlayDiveImpact()
- Added a dive-start reaction in AdvanceBoth: on head crossing the hole mouth call _holeView.BeginDive() and fire HapticService.Play(SoftImpact) (dive ON only)

Arrow.cs — [MOD]
- Added IsCrawlAudible (IsCrawling && !AllLimbsInHole) and the AllLimbsInHole getter so the crawl SFX stops once all four limbs enter the hole
- Added NotifyAbilitiesBlockedClick() fanning OnBlockedClick() to every ability with CanClicked() == false
- Updated the _holeView field comment for the new Dismiss ownership

Arrow.Input.cs — [MOD]
- Called NotifyAbilitiesBlockedClick() on the blocked-tap path so each blocker layers its own juice on top of the red-flash + "wrong" feedback

Arrow.Visual.cs — [MOD]
- Drew the tail as a per-arrow mesh (TailSprite/TailMatrix/TailVisible API) instead of a SpriteRenderer so the hoa_tiet motifs render above it; disabled tailRenderer after capturing the default sprite
- Added per-ColorId _activeTailSprite selection in ApplyLimbAndPatternArt
- Rebuilt UpdateArrowTail to compute a _tailMatrix TRS (native quad x uniform scale) instead of driving tailRenderer.transform; SetTailVisible now sets _tailVisible
- Stored _flashAmount and fired EventService.OnBlockedFlash inside PlayBlockedFlash to echo the blink on the HUD

ArrowPictureService.cs — [MOD]
- Drove the crawl loop with IsCrawlAudible instead of IsCrawling in UpdateArrowAudio
- Added DrawTails() (one Graphics.DrawMesh per arrow with a per-draw MPB for the blocked-flash) and EnsureTailRes with wide no-clip bounds + _RimMode=2
- Split render queues so order always holds: tail 3000 -> 3001 and motifs 3001 -> 3002 (body 3000 < tail 3001 < motif 3002), fixing the tail sometimes drawing under the body

MapSpawner.cs — [MOD]
- Reduced HideHoles to a safety-net ReturnAll on win now that each hole self-dismisses

----------------------------------------------------------------------

2. Gameplay — Obstacles

IObstacleAbility.cs — [MOD]
- Added OnBlockedClick() to the interface — a tap while this ability is still blocking

ObstacleAbility.cs — [MOD]
- Added a virtual no-op OnBlockedClick()

IceObstacleAbility.cs — [MOD]
- Overrode OnBlockedClick(): shake the head icon (pokeShakeStrength/pokeShakeDuration) + play IceBreak while still frozen; no particle sweep (reserved for the actual thaw)
- Added _pokeShakeTween with reset in OnReturnToPool

LockObstacleAbility.cs — [MOD]
- Overrode OnBlockedClick(): rattle lockRenderer (shake) + optional pokeSoundKey while still locked
- Added _pokeShakeTween with reset in OnReturnToPool

----------------------------------------------------------------------

3. UI Layer / HUD

InGameScreen.cs — [MOD]
- Added a low-time red edge warning: lazily built left/right gradient Image bars (raycastTarget off), blinking once per second via FlashSideBlinkOnce when 0 < TimeRemaining <= lowTimeThreshold
- Added runtime MakeEdgeGradientSprite (quadratic edge falloff) and DestroyGradientSprite cleanup
- Added FlashBlockedScreenOnce() echoing a blocked tap on the same bars (fainter, blockedScreenFlashColor), subscribed to EventService.OnBlockedFlash and yielding while the low-time warning is active
- Hid the Restart button during the FirstPlay tutorial via a lazily-added CanvasGroup (SetReplayVisible), subscribing TutorialService.StepChanged/TutorialEnded; guarded OnReplayClicked with IsFirstPlayTutorialRunning()
- Stopped all tweens and unsubscribed events in OnDestroy

----------------------------------------------------------------------

4. Services

EventService.cs — [MOD]
- Added the OnBlockedFlash PEvent raised on every blocked-tap red-rim blink

----------------------------------------------------------------------

5. Config / ScriptableObjects

GameplayConfigSO.cs — [MOD]
- Added Low Time Warning fields (lowTimeThreshold/lowTimeFlashColor/lowTimeFlashDuration/lowTimeFlashSideWidth)
- Added Blocked Screen Flash fields (blockedScreenFlashEnabled/blockedScreenFlashColor)

ColorConfigSO.cs — [MOD]
- Added a per-ColorId tailSprite field

ColorConfigSOEditor.cs — [MOD]
- Added the tailSprite row to the custom inspector

GameplayConfigSO.asset — [MOD]
- Serialized winTriggerDelay 0.35, the lowTime* defaults, and the blockedScreenFlash defaults (enabled, alpha ~0.22)

GeckoConfigSO.asset — [MOD]
- Set holeExitOffset 0.2 -> 1 (hole farther from the grid)
- Set tailSize 0 -> 0.94 for the per-color tail sprite

ColorConfigSo.asset — [MOD]
- Wired tailSprite for all 16 ColorIds from asset_gecko-1/<color>/tail.png

----------------------------------------------------------------------

6. Shaders

BoundMaskTransparent.shader — [MOD]
- Added _RimMode=2 tail mode: an SDF-capsule outline over the three free sides + rounded cap, excluding the connecting edge, matching the body rim width/profile

PreloaderBackground.shader — [MOD]
- Declared [PerRendererData] _MainTex to silence the uGUI "doesn't have a texture property _MainTex" warning

----------------------------------------------------------------------

7. Prefab Updates

Hole.prefab — [MOD]
- Tuned diveScale to 1.2 and migrated squashPunch/squashDuration serialization to popOutScale/popOutDuration

InGameScreen.prefab — [MOD]
- Swapped the Pause/Restart sibling order so Pause is now the outermost button

ConfirmPopup.prefab / LevelFailedPopup.prefab / LevelPausePopup.prefab — [MOD]
- Updated popup layout

----------------------------------------------------------------------

8. Art & Audio

BG_ingame.png — [MOD]
- Updated the in-game background

GeckoWalk.ogg — [MOD]
- Replaced the gecko walk SFX clip

----------------------------------------------------------------------

9. Docs

Gameplay.md — [MOD]
- Documented hole self-dismiss + dive-in juice, the low-time and blocked-tap edge flashes, the IsCrawlAudible SFX gate, obstacle OnBlockedClick, the tail mesh render order (body < tail < motif), and Restart-hidden-in-tutorial
- Logged two open root causes: reload does not reset TutorialService, and TutorialOverlay's canvas draws above UIService popups
```
