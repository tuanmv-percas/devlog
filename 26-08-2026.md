```
1. Analytics - Percas SDK Wiring

AnalyticsService.cs - [MOD]
- Reconnected the game's AnalyticsService (previously a no-op after TinySauce/Voodoo SDK removal) to Percas SDK; Track / OnGameStarted / OnGameOnGameFinished now forward to PercasSDK.Analytics
- Added PercasEventAdapter, a private adapter mapping the game's Analytics.ITrackingEvent to Percas.SDK.Domain.ITrackingEvent 1-1 (same EventName + Properties shape, different namespace) so no game-side interface change was needed
- Guarded every call with PercasSDK.IsInitialized to avoid NRE when a gameplay scene is entered directly in Editor without the Splash scene
- Added objectivesLeftPercent optional parameter to OnGameOnGameFinished, forwarded to TrackGameFinished (SDK emits level_objectives_left only on fail)
- Added a static Log helper for per-level [Analytics] tracing; iterated its gating three times in one day: first gated behind DEVELOP_BUILD/UNITY_EDITOR, then made unconditional to verify on any build, then re-gated behind [Conditional("DEVELOP_BUILD")]/UNITY_EDITOR for production

----------------------------------------------------------------------

2. Level Context - Objectives-Left Metric

LevelContextService.cs - [NEW]
- New service holding the current level's context for analytics and future logic; exposes RemainingArrows, TotalArrows, and GetObjectivesLeftPercent()
- GetObjectivesLeftPercent returns 0..100 (0 = near clear, 100 = nothing cleared) computed from ActiveArrows.Count over LevelData.ValidArrowCount; returns null when data is insufficient so callers skip the event rather than send a wrong number
- ResolveArrowService falls back to FindObjectOfType because ArrowPictureService is not registered in ServiceLocator

LevelObjectives.cs - [NEW then removed]
- Created as a static helper (ArrowsLeftPercent) for the objectives-left calc, then deleted the same day; the logic was moved into LevelContextService so call sites depend on a registered service instead of a static

GameContext.cs - [MOD]
- Registered LevelContextService in the ServiceLocator setup

GlobalStateMachine.LevelFailedState.cs - [MOD]
- On level fail, pass LevelContextService.GetObjectivesLeftPercent() into OnGameOnGameFinished (initially via LevelObjectives.ArrowsLeftPercent, then switched to the service)

InGameScreen.cs - [MOD]
- On mid-level replay (counts as a failed attempt), pass the objectives-left percent into OnGameOnGameFinished, same source as the fail state

----------------------------------------------------------------------

3. Arrow Picture System

Arrow.cs - [MOD]
- Added _committed state field: true once the arrow is guaranteed to exit (no stationary Idle/Blocked cell remains anywhere on its exit lane); reset in ResetRuntimeState
- Documented the invariant distinguishing _committed (gates home release / reverse) from _hasConfirmedMoving (gates bomb-step/win accounting)

Arrow.Func.cs - [MOD]
- Reserve the home (path) cells as an Idle mark until exit is guaranteed: StartMoving sets _committed from IsHeadBlockedByStationary(), AnimateForward re-checks every frame and calls Commit() the instant the lane clears
- Added Commit(): re-stamps reserved home cells from Idle to the live move state and releases tail cells held during the reservation
- Gated ReleaseTailCells behind _committed so an uncommitted arrow keeps its whole path reserved and can reverse straight home with nothing to re-claim
- SetOwnedCellsState now pins path cells to Idle while uncommitted, only sweeping exit-lane cells to the move state
- A committed arrow no longer reverses on hitting a stationary blocker; it holds instead (recoverable, never a grid-corrupting overlap)

ArrowPictureService.cs - [MOD]
- On win, ease the camera back to its default resting framing (AnimateToRestingAsync) before the dot reveal/vanish when the player had pinch-zoomed/panned; no-op when already at rest
- Restored a single scale-up reveal wave (dots, useGem:false) before the dot-vanish wave, driven by winCameraResetDuration

----------------------------------------------------------------------

4. Camera

CameraZoomHandler.cs - [MOD]
- Added AnimateToRestingAsync(duration, ct): smoothstep-lerps camera size/position back to the resting framing (MinSize on zoomable grids, ComfortSize on small ones, centred on grid) while blocking zoom/pan input; returns immediately when already at rest so an un-zoomed win pays nothing
- Distinct from AnimateToHomeAsync (which targets the more zoomed-out appear framing); this purely undoes a manual pinch-zoom/pan

GameplayConfigSO.cs - [MOD]
- Added winCameraResetDuration (default 0.35s) controlling the win-time camera-reset animation

----------------------------------------------------------------------

5. Obstacles - Key Lock

LockObstacleAbility.cs - [MOD]
- Bake Spine frame 0 (Update(0f) + LateUpdate) right after activating the lock skeleton in the unlock animation, so the mesh renders at the unlock animation's first frame instead of flashing the setup pose (key sitting in lock) for one frame

----------------------------------------------------------------------

6. UI Layer

LayoutToggleSetting.cs - [MOD]
- Replaced the inline SFX toggle handler with OnSfxToggleChanged so the toggle's own click sound stays audible when muting: enable sound immediately when turning on; when turning off, defer the mute one frame (MuteSoundNextFrameAsync via UniTask.NextFrame) so the click SFX fires while SoundOn is still true

----------------------------------------------------------------------

7. Build - Editor

BuildConfig.cs - [MOD]
- Added RequiredSdkDefines (FIREBASE_ANALYTICS, FIREBASE_CRASHLYTICS, FIREBASE_REMOTE_CONFIG, ADJUST, FACEBOOK_SDK, GAME_ANALYTICS) and force them into every Android build, since ApplyDefines overwrites all Android defines and the EditorPrefs list was seeded before the SDK import
- Merge every existing ProjectSettings (Android) define not already in the list (default ON) so the list stays a superset and no externally-added define (provider symbols, DEVELOP_BUILD) is dropped on build

DisableBitcode.cs - [MOD]
- Wrapped the Percas SDK Facebook post-process file in #if UNITY_EDITOR so it no longer compiles into the iOS player build

iOSInfoPlistPostProcess.cs - [MOD]
- Changed the guard from #if UNITY_IOS to #if UNITY_EDITOR && UNITY_IOS to fix the iOS player build

Percas.SDK.Editor.asmdef - [MOD]
- Updated assembly definition Percas.SDK.Editor to include only the Editor platform, keeping Editor code out of player builds

----------------------------------------------------------------------

8. Percas SDK - Library Import

Assets/Percas SDK/** - [NEW]
- Imported the Percas SDK (Analytics, Attribution, AdRevenue, Subscription, Deeplink, RemoteConfig, Crashlytics) with Domain interfaces/models, Application services, and Adjust/Firebase/Facebook/GameAnalytics infrastructure providers, plus setup wizard and defines manager
- Bundled vendor SDKs: Adjust, Firebase (Analytics/Crashlytics/RemoteConfig/Messaging 12.10.0), GameAnalytics, Facebook SDK; External Dependency Manager bumped 1.2.185 to 1.2.188

----------------------------------------------------------------------

9. Config / Environment

PercasSDKConfig.asset / AdjustConfig / FacebookConfig / GameAnalyticsConfig / FirebaseConfig - [NEW/MOD]
- Configured Percas SDK provider keys for Gecko Jam; set Adjust environment to Sandbox
- GameAnalytics Settings.asset configured

google-services.json / GoogleService-Info.plist / google-services-desktop.json / FirebaseApp.androidlib - [NEW]
- Added Firebase config for the gecko-jam project (Android, iOS, desktop, and generated androidlib manifest/resources)

AndroidManifest.xml - [MOD]
- Restored VIBRATE and network permissions and Facebook provider authority

ProjectSettings.asset - [MOD]
- Added iOS provider scripting defines for the Percas SDK

Packages/manifest.json / packages-lock.json - [MOD]
- Added com.unity.ads.ios-support 1.2.0 for iOS

----------------------------------------------------------------------

10. Prefab Updates

ClothOffscreenBubble.prefab - [MOD]
- Increased the arrow size shown in the offscreen bubble

UnlockBubble.prefab - [MOD]
- Increased the arrow size shown in the unlock bubble

----------------------------------------------------------------------

11. Assets

icon1024.png - [MOD]
- Updated the app icon

----------------------------------------------------------------------

12. Docs

Gameplay.md - [MOD]
- Documented the win-flow camera reset (AnimateToRestingAsync / winCameraResetDuration), the PlayWinCelebrationAsync / PlayDotVanishAsync split, and the new winDotVanish* / winCameraResetDuration config rows
- Documented the _committed "reserve home mark until exit is guaranteed" model, the hold-instead-of-reverse rule, the invariant proof, and the now-dormant chain-reverse fallback
```
